{
  "name": "breachledger",
  "version": "1.0.0",
  "private": true,
  "description": "A structured public record of disclosed U.S. data breaches, with a remediation engine and litigation tracking. Every published fact traces to a citable government or court source.",
  "type": "commonjs",
  "workspaces": [
    "packages/*",
    "workers/*"
  ],
  "scripts": {
    "build": "node site/build.js",
    "db:migrate": "wrangler d1 migrations apply breachledger --local",
    "db:migrate:remote": "wrangler d1 migrations apply breachledger --remote",
    "db:seed": "node packages/schema/seed/load.js --out .wrangler/tmp-seed.sql && wrangler d1 execute breachledger --local --file .wrangler/tmp-seed.sql",
    "db:seed:remote": "node packages/schema/seed/load.js --out .wrangler/tmp-seed.sql && wrangler d1 execute breachledger --remote --file .wrangler/tmp-seed.sql",
    "db:verify": "wrangler d1 execute breachledger --local --command \"SELECT 'data_classes' AS tbl, COUNT(*) AS rows FROM data_classes UNION ALL SELECT 'state_rights', COUNT(*) FROM state_rights UNION ALL SELECT 'remediation_modules', COUNT(*) FROM remediation_modules UNION ALL SELECT 'breaches', COUNT(*) FROM breaches UNION ALL SELECT 'sources', COUNT(*) FROM sources UNION ALL SELECT 'litigation', COUNT(*) FROM litigation\"",
    "db:reset": "rm -rf .wrangler/state && npm run db:migrate && npm run db:seed",
    "test": "node --test packages/severity/score.test.js packages/ingest/entity-resolve.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
